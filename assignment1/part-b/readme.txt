The link tag is used to link css file and the favicon
nav tag is used for the navbar
header tag used on the index.html file has multiple children and the entire container is styled using flex. The children include an image, video tag which shows the trailer of the movie, a button which redirects to the checkout page and few tags to show the text. The header tag on the checkout page includes only the header text checkout.
There are two section tag used on the index.html page. The first one tag is for about the movie and the second tag is for the movie reviews it has a audio tag as a child when clicked on it plays a audio.
There is a footer which has © text and a background color
The checkout page has a form which accepts users email and card details and it has a button of type submit 
